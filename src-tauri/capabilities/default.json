{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Commands the window may call. Plugin commands are denied unless listed here; the app's own #[tauri::command] functions are not gated.",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
