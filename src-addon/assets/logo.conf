# ES draws resources/logo.png whenever it is loading -- every game launch and
# every return from one. There is no setting for it, so the file is replaced
# and put back at each boot by boot-custom.sh.
